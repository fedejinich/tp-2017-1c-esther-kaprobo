/*
 * leerEscribirMemoria.c
 *
 *  Created on: 2/7/2017
 *      Author: utnso
 */


#include "leerEscribirMemoria.h"
/*
void almacenarBytesEnMemoria(t_direccion* direccion, void* buffer) {
	t_almacenarBytes* almacenarBytes = malloc(sizeof(t_almacenarBytes));

	almacenarBytes->pid = pcb->pid;
	almacenarBytes->pagina = direccion->pagina;
	almacenarBytes->offset = direccion->offset;
	almacenarBytes->tamanio = direccion->size;
	almacenarBytes->buffer = buffer;

	enviar(memoria, ALMACENAR_BYTES, almacenarBytes->tamanio, almacenarBytes);
}

void* solicitarBytesAMemoria(t_direccion* direccion) {
	t_solicitudBytes* solicitarBytes = malloc(sizeof(t_almacenarBytes));

	solicitarBytes->pid = pcb->pid;
	solicitarBytes->pagina = direccion->pagina;
	solicitarBytes->offset = direccion->offset;
	solicitarBytes->tamanio = direccion->size;

	enviar(memoria, SOLICITAR_BYTES, solicitarBytes->tamanio, solicitarBytes);
}
*/
